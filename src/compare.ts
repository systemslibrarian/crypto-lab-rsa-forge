/**
 * compare.ts — Panel 6: RSA vs ECC vs Post-Quantum.
 * Static data; no dynamic computation needed.
 * The table and bar chart are already in the HTML.
 * This module wires up any interactive elements if added in future.
 */

export function initComparePanel(): void {
  // Panel 6 is fully static HTML — no JS initialization required.
  // Placeholder for any future interactive comparison features.
}
